# VoiceShield Model Weights Directory

Place your trained AASIST PyTorch model weights file here:

`aasist.pth`

When model weights are placed in this folder, the backend model service automatically detects and loads the model for production inference.
